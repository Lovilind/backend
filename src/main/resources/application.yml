spring:
  application:
    name: lovlind
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/expense?useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true
    password: wkaqh12Q!
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: false
  jpa:
    hibernate:
      ddl-auto: none
  data:
    redis:
      host: 127.0.0.1   #${REDIS_URL}
      port: 6379
